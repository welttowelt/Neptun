[package]
name = "defi-backend"
version = "0.1.0"
edition = "2021"
[dependencies]
actix-web = "4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ethers = { version = "1.0", features = ["abigen"] }
tokio = { version = "1", features = ["full"] }

[dependencies]
